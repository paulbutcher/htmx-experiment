(ns paulbutcher.htmx-experiment-test
  (:require [clojure.test :refer :all]
            [paulbutcher.htmx-experiment :refer :all]))

;; (deftest a-test
;;   (testing "FIXME, I fail."
;;     (is (= 0 1))))
